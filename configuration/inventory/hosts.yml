all:
  children:
    # MANAGEMENT ZONE (Tailscale Network)
    management:
      hosts:
        ops-center:
          # No IP needed! MagicDNS handles it. 
    
    # KUBERNETES ZONE (Local Network)
    kubernetes:
      children:
        masters:
          hosts:
            server:
              ansible_host: 192.168.0.21
        workers:
          hosts:
            node-0:
              ansible_host: 192.168.0.22
            node-1:
              ansible_host: 192.168.0.23

all:
  children:
    # -----------------------------------
    # INFRASTRUCTURE (The Hypervisor)
    # -----------------------------------
    hypervisor:
      hosts:
        pve:
          ansible_host: 192.168.0.3
          ansible_user: devops
    
    # -----------------------------------
    # ZONE MGMT
    # -----------------------------------
    management:
      hosts:
        ops-center:
          ansible_host: 192.168.0.5

    # -----------------------------------
    # ZONE PROD
    # -----------------------------------
    production:
      hosts:
        k3s-prod:
          ansible_host: 192.168.0.30

    # -----------------------------------
    # ZONE LAB (K8s The Hard Way)
    # -----------------------------------
    lab:
      children:
        load_balancers:
          hosts:
            gateway:
              ansible_host: 192.168.0.10
        k8s_cluster:
          hosts:
            server:
              ansible_host: 192.168.0.21
            node-0:
              ansible_host: 192.168.0.22
            node-1:
              ansible_host: 192.168.0.23