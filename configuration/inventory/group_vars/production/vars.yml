---
# ----------------------------------------------------------------
# CONFIGURATION - PLAIN TEXT
# ----------------------------------------------------------------
# Map the vault variables to usable names here
grafana_admin_password: "{{ vault_grafana_admin_password }}"
discord_webhook_url: "{{ vault_discord_webhook_url }}"

proxmox_api_token_secret: "{{ vault_proxmox_api_token_secret }}"
proxmox_gateway_password: "{{ vault_proxmox_gateway_password }}"

# MinIO Secrets
# minio_access_key: "{{ vault_minio_access_key }}"
# minio_secret_key: "{{ vault_minio_secret_key }}"
ops_center_tailscale_ip: "100.87.130.97"
# OCI Object Storage S3 Credentials (Replaces MinIO)
oci_s3_access_key: "{{ vault_oci_s3_access_key }}"
oci_s3_secret_key: "{{ vault_oci_s3_secret_key }}"

# Other config
