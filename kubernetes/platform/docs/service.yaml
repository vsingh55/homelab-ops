apiVersion: v1
kind: Service
metadata:
  name: homelab-docs
  namespace: platform
  labels:
    app.kubernetes.io/name: homelab-docs
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app.kubernetes.io/name: homelab-docs
